export const Role = ({ className }) => (
  <>
    <span
      className={`blue-gradient tracking-tighter extra-bold ${className ?? ""}`}
    >
      Full Stack
    </span>
    <span
      className={`blue-gradient tracking-tighter extra-bold ${className ?? ""}`}
    >
      Web Developer
    </span>
  </>
);

export default Role;
