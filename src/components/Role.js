export const Role = ({ className }) => (
  <>
    <span className={`blue-gradient extra-bold ${className ?? ""}`}>
      Full Stack
    </span>
    <span className={`blue-gradient extra-bold ${className ?? ""}`}>
      Web Developer
    </span>
  </>
);

export default Role;
